"use client";
import { FC } from "react";
import "./Button.scss";

const Button: FC = () => {
  return (
    <a className="download-btn" href="#">
      Резюме
    </a>
  );
};

export default Button;
